pub struct Chapter {
    pub id: String,
    pub title: String,
    pub number: f32,
    pub volume: Option<u32>,
    pub pages: u32,
    pub read: bool,
    pub scanlator: String,
    pub upload_date: String,
}
