import type { FC } from "react";

const Manga: FC = () => {
	return (
		<div className="flex h-full w-full items-center justify-center">
			<p className="text-2xl font-bold">Manga is under construction</p>
		</div>
	);
};

export default Manga;
